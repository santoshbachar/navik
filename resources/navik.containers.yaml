

driver: docker
using: API,CMD
common_args: --detach --rm
port_pool_range: 8090:8100
containers:
  - name: mongo
    state:
      min: 1
      max: 10
    args:
      - -p 8090:27017
#      - -v /home/santoshbachar:/home
#      - -v /home/santoshbachar/navik:/home/lib
#      - --log-driver=fluentd
